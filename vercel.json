{
  "installCommand": "gem install bundler && bundle install",
  "buildCommand": "bundle exec jekyll build",
  "outputDirectory": "_site",
  "crons": [
    {
      "path": "/api/trigger-daily-summary",
      "schedule": "0 22 * * *"
    }
  ]
}
